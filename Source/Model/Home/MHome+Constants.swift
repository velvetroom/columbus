import Foundation

extension MHome
{
    struct Constants
    {
        struct TimeIntervals
        {
            static let waitAddNavigation:TimeInterval = 1
        }
        
        struct Tile
        {
            static let template:String = "{x}.{y}.{z}"
        }
    }
}
